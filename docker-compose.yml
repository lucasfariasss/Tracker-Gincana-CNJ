services:
  frontend:
    build: .
    container_name: premio-cnj-frontend
    restart: always
    ports:
      - "8080:80" # acesse em http://localhost:8080
    volumes:
      - ./data:/usr/share/nginx/html/data:ro